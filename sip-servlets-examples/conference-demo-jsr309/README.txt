Simple Conference Application
-----------------------------

Before building make sure you have Media Server 1.0.0.CR2 installed.

Set JBOSS_HOME and run mvn clean install -Pdeploy to deploy.

To use:
Use some SIP phones to dial sip-servlets-conference@localhost:5080 (or change according to your hostname and port)
