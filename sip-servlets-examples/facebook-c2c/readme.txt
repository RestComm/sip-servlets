1. Set the JBOSS_HOME environment variable to point to JBoss AS location with sip servlets installed
2. Run "mvn clean install war:war"
3. In the web.xml descriptor put the passwords (sorry we dont give these out).

Configure the Application as a facebook iframe application.